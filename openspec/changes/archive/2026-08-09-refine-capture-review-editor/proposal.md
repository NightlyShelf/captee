## Why

Capture review should feel like one clear editor, not separate context and annotation areas. Keep the selected capture and annotation draft when Modify reopens selection. Remove distracting coordinates and make confirmation return the user to the inserted image and current preview. Repository rules will require proposal discussion, per-subtask commits and pushes, then user review before archive and PR.
